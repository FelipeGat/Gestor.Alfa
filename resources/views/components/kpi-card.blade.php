<div class="bg-white shadow rounded-xl p-6 flex flex-col items-center justify-center">
    <div class="text-xs text-gray-500 mb-1">{{ $titulo ?? 'KPI' }}</div>
    <div class="text-2xl font-bold text-gray-800">{{ $valor ?? '-' }}</div>
</div>
